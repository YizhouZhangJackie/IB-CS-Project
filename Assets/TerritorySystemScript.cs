using UnityEngine;

public class TerritorySystemScript : MonoBehaviour
{
    private int leftBound;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        leftBound = 3;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
